import 'package:flutter/cupertino.dart';

class Subscription{
  String title;
  String avg;
  double availableMoney;
  Icon iconTitle;

  Subscription(this.title, this.avg, this.availableMoney, this.iconTitle);
}